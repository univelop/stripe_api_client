name: stripe_api_client
description: Stripe API Client generated from OpenAPI specification
version: 0.1.0
homepage: https://github.com/univelop/stripe_api_client
repository: https://github.com/univelop/stripe_api_client

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  # Dependencies will be added by the generated code
  build: ^2.4.0

dev_dependencies:
  build_runner: ^2.4.0
  http: ^1.2.0
  path: ^1.9.0
