// ignore_for_file: type=lint
/// auto generated
///  The reason the review was opened. One of `rule` or `manual`.
enum ReviewOpenedReason {
  manual('manual'),
  rule('rule');

  const ReviewOpenedReason(this.value);
  final String value;
}
