// ignore_for_file: type=lint
/// auto generated
///  The status of the Tax `Settings`.
enum SettingsStatus {
  active('active'),
  pending('pending');

  const SettingsStatus(this.value);
  final String value;
}
