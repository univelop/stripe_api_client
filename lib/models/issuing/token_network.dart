// ignore_for_file: type=lint
/// auto generated
///  The token service provider / card network associated with the token.
enum TokenNetwork {
  mastercard('mastercard'),
  visa('visa');

  const TokenNetwork(this.value);
  final String value;
}
