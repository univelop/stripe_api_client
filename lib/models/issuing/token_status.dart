// ignore_for_file: type=lint
/// auto generated
///  The usage state of the token.
enum TokenStatus {
  active('active'),
  deleted('deleted'),
  requested('requested'),
  suspended('suspended');

  const TokenStatus(this.value);
  final String value;
}
