// ignore_for_file: type=lint
/// auto generated
///  The UI mode of the Session. Defaults to `hosted`.
enum SessionUiMode {
  custom('custom'),
  embedded('embedded'),
  hosted('hosted');

  const SessionUiMode(this.value);
  final String value;
}
