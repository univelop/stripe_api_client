// ignore_for_file: type=lint
import 'package:microsoft_kiota_abstractions/microsoft_kiota_abstractions.dart';
import './plans_post_request_body_billing_scheme.dart';
import './plans_post_request_body_interval.dart';
import './plans_post_request_body_product.dart';
import './plans_post_request_body_tiers.dart';
import './plans_post_request_body_tiers_mode.dart';
import './plans_post_request_body_transform_usage.dart';
import './plans_post_request_body_usage_type.dart';

/// auto generated
class PlansPostRequestBody implements Parsable {
  ///  Whether the plan is currently available for new subscriptions. Defaults to `true`.
  bool? active;

  ///  A positive integer in cents (or local equivalent) (or 0 for a free plan) representing how much to charge on a recurring basis.
  int? amount;

  ///  Same as `amount`, but accepts a decimal value with at most 12 decimal places. Only one of `amount` and `amount_decimal` can be set.
  String? amountDecimal;

  ///  Describes how to compute the price per period. Either `per_unit` or `tiered`. `per_unit` indicates that the fixed amount (specified in `amount`) will be charged per unit in `quantity` (for plans with `usage_type=licensed`), or per unit of total usage (for plans with `usage_type=metered`). `tiered` indicates that the unit pricing will be computed using a tiering strategy as defined using the `tiers` and `tiers_mode` attributes.
  PlansPostRequestBodyBillingScheme? billingScheme;

  ///  Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  String? currency;

  ///  Specifies which fields in the response should be expanded.
  Iterable<String>? expand;

  ///  An identifier randomly generated by Stripe. Used to identify this plan when subscribing a customer. You can optionally override this ID, but the ID must be unique across all plans in your Stripe account. You can, however, use the same plan ID in both live and test modes.
  String? id;

  ///  Specifies billing frequency. Either `day`, `week`, `month` or `year`.
  PlansPostRequestBodyInterval? interval;

  ///  The number of intervals between subscription billings. For example, `interval=month` and `interval_count=3` bills every 3 months. Maximum of three years interval allowed (3 years, 36 months, or 156 weeks).
  int? intervalCount;

  ///  Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  String? metadata;

  ///  The meter tracking the usage of a metered price
  String? meter;

  ///  A brief description of the plan, hidden from customers.
  String? nickname;

  ///  The product property
  PlansPostRequestBodyProduct? product;

  ///  Each element represents a pricing tier. This parameter requires `billing_scheme` to be set to `tiered`. See also the documentation for `billing_scheme`.
  Iterable<PlansPostRequestBodyTiers>? tiers;

  ///  Defines if the tiering price should be `graduated` or `volume` based. In `volume`-based tiering, the maximum quantity within a period determines the per unit price, in `graduated` tiering pricing can successively change as the quantity grows.
  PlansPostRequestBodyTiersMode? tiersMode;

  ///  Apply a transformation to the reported usage or set quantity before computing the billed price. Cannot be combined with `tiers`.
  PlansPostRequestBodyTransformUsage? transformUsage;

  ///  Default number of trial days when subscribing a customer to this plan using [`trial_from_plan=true`](https://docs.stripe.com/api#create_subscription-trial_from_plan).
  int? trialPeriodDays;

  ///  Configures how the quantity per period should be determined. Can be either `metered` or `licensed`. `licensed` automatically bills the `quantity` set when adding it to a subscription. `metered` aggregates the total usage based on usage records. Defaults to `licensed`.
  PlansPostRequestBodyUsageType? usageType;

  /// Creates a new instance of the appropriate class based on discriminator value
  ///  [parseNode] The parse node to use to read the discriminator value and create the object
  static PlansPostRequestBody createFromDiscriminatorValue(
      ParseNode parseNode) {
    return PlansPostRequestBody();
  }

  /// The deserialization information for the current model
  @override
  Map<String, void Function(ParseNode)> getFieldDeserializers() {
    var deserializerMap = <String, void Function(ParseNode)>{};
    deserializerMap['active'] = (node) => active = node.getBoolValue();
    deserializerMap['amount'] = (node) => amount = node.getIntValue();
    deserializerMap['amount_decimal'] =
        (node) => amountDecimal = node.getStringValue();
    deserializerMap['billing_scheme'] = (node) => billingScheme =
        node.getEnumValue<PlansPostRequestBodyBillingScheme>((stringValue) =>
            PlansPostRequestBodyBillingScheme.values
                .where((enumVal) => enumVal.value == stringValue)
                .firstOrNull);
    deserializerMap['currency'] = (node) => currency = node.getStringValue();
    deserializerMap['expand'] =
        (node) => expand = node.getCollectionOfPrimitiveValues<String>();
    deserializerMap['id'] = (node) => id = node.getStringValue();
    deserializerMap['interval'] = (node) => interval =
        node.getEnumValue<PlansPostRequestBodyInterval>((stringValue) =>
            PlansPostRequestBodyInterval.values
                .where((enumVal) => enumVal.value == stringValue)
                .firstOrNull);
    deserializerMap['interval_count'] =
        (node) => intervalCount = node.getIntValue();
    deserializerMap['metadata'] = (node) => metadata = node.getStringValue();
    deserializerMap['meter'] = (node) => meter = node.getStringValue();
    deserializerMap['nickname'] = (node) => nickname = node.getStringValue();
    deserializerMap['product'] = (node) => product =
        node.getObjectValue<PlansPostRequestBodyProduct>(
            PlansPostRequestBodyProduct.createFromDiscriminatorValue);
    deserializerMap['tiers'] = (node) => tiers =
        node.getCollectionOfObjectValues<PlansPostRequestBodyTiers>(
            PlansPostRequestBodyTiers.createFromDiscriminatorValue);
    deserializerMap['tiers_mode'] = (node) => tiersMode =
        node.getEnumValue<PlansPostRequestBodyTiersMode>((stringValue) =>
            PlansPostRequestBodyTiersMode.values
                .where((enumVal) => enumVal.value == stringValue)
                .firstOrNull);
    deserializerMap['transform_usage'] = (node) => transformUsage =
        node.getObjectValue<PlansPostRequestBodyTransformUsage>(
            PlansPostRequestBodyTransformUsage.createFromDiscriminatorValue);
    deserializerMap['trial_period_days'] =
        (node) => trialPeriodDays = node.getIntValue();
    deserializerMap['usage_type'] = (node) => usageType =
        node.getEnumValue<PlansPostRequestBodyUsageType>((stringValue) =>
            PlansPostRequestBodyUsageType.values
                .where((enumVal) => enumVal.value == stringValue)
                .firstOrNull);
    return deserializerMap;
  }

  /// Serializes information the current object
  ///  [writer] Serialization writer to use to serialize this model
  @override
  void serialize(SerializationWriter writer) {
    writer.writeBoolValue('active', value: active);
    writer.writeIntValue('amount', amount);
    writer.writeStringValue('amount_decimal', amountDecimal);
    writer.writeEnumValue<PlansPostRequestBodyBillingScheme>(
        'billing_scheme', billingScheme, (e) => e?.value);
    writer.writeStringValue('currency', currency);
    writer.writeCollectionOfPrimitiveValues<String?>('expand', expand);
    writer.writeStringValue('id', id);
    writer.writeEnumValue<PlansPostRequestBodyInterval>(
        'interval', interval, (e) => e?.value);
    writer.writeIntValue('interval_count', intervalCount);
    writer.writeStringValue('metadata', metadata);
    writer.writeStringValue('meter', meter);
    writer.writeStringValue('nickname', nickname);
    writer.writeObjectValue<PlansPostRequestBodyProduct>('product', product);
    writer.writeCollectionOfObjectValues<PlansPostRequestBodyTiers>(
        'tiers', tiers);
    writer.writeEnumValue<PlansPostRequestBodyTiersMode>(
        'tiers_mode', tiersMode, (e) => e?.value);
    writer.writeObjectValue<PlansPostRequestBodyTransformUsage>(
        'transform_usage', transformUsage);
    writer.writeIntValue('trial_period_days', trialPeriodDays);
    writer.writeEnumValue<PlansPostRequestBodyUsageType>(
        'usage_type', usageType, (e) => e?.value);
  }
}
